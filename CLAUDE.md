Dotbot-managed dotfiles repo. Symlinks are configured in install.conf.yaml.
Files here map to ~/.config/ and ~/ — not used directly from this directory.

Do not read ~/.config/zsh/.zshrc.local — it contains local overrides and secrets.
Do not commit secrets, tokens, or machine-specific paths.
